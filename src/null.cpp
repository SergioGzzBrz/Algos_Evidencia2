
using namespace std;

/// @brief Realiza una comparación simple para tener información en el archivo
/// @param number Numero de prueba
/// @return `true` si el numero es igual a 1, de lo contrario `false`.
bool empty_function(int number)
{
    return (number == 1);
}
